allprojects {
    group = 'ome'
    version = '0.1.0'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}

task wrapper(type: Wrapper) {
    description 'Sets up the Gradle Wrapper.'
    gradleVersion = '2.12'
}

